import { FC } from 'react';
interface Props {
    selected: boolean;
    activeColor: string;
    inactiveColor: string;
    size: number | string;
}
declare const Star: FC<Props>;
export default Star;
