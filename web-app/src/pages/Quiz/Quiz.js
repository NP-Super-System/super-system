import React from 'react';

import styles from './Quiz.module.css';

const Quiz = props=>{

    return (
        <div className='container'>
            Quiz
        </div>
    );
}

export default Quiz;